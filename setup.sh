#!/bin/bash

# script to install CMake and other dependencies used in this course

brew install cmake
brew install ninja
brew install GCC
brew install llvm
brew install --cask gcc-arm-embedded
brew install pkg-config
brew install doxygen cppcheck clang-format gcovr lcov